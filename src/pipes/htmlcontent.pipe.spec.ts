import { HtmlcontentPipe } from './htmlcontent.pipe';

describe('HtmlcontentPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlcontentPipe();
    expect(pipe).toBeTruthy();
  });
});
